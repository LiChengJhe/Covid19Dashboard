import { SymbolNumberPipe } from './symbol-number.pipe';

describe('SymbolNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new SymbolNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
